<div class="row pt-3">
  <%= render 'layouts/calculator', farmer: @farmer %>
  <div class="col-md-1"></div>
  <div class="col-md-7">
    <div class="d-flex justify-content-center">
      <h1 class="text-danger"><i class="fa-solid fa-triangle-exclamation"></i> 注意 <i class="fa-solid fa-triangle-exclamation"></i></h1>
    </div>
    <div class="col">
      <h2 class="d-flex justify-content-center">退会すると、会員登録情報や</h2>
      <h2 class="d-flex justify-content-center">これまでに登録された各情報が閲覧できなくなります。</h2>
      <h2 class="d-flex justify-content-center">退会する場合は、以下「退会する」をクリックしてください。</h2>
    </div>

    <% unless @farmer.guest? %>
      <div class="row pt-5">
        <div class="col">
          <div class="d-flex justify-content-center"><%= link_to '退会しない', edit_farmer_current_farmer_path(current_farmer), class:"btn btn-primary btn-lg" %></div>
        </div>
        <div class="col">
          <div class="d-flex justify-content-center"><%= link_to '退会する', farmer_current_farmer_path(current_farmer), method: :delete, "data-confirm" => "本当に退会しますか？", class:"btn btn-danger btn-sm" %></div>
        </div>
      </div>
    <% else %>
      <p>ゲストユーザー編集は行えません。</p>
    <% end %>
  </div>
</div>