<!DOCTYPE html>
<html>
  <head>
    <title>Noukou</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>
  
  <body>
    <header>
      <nav class="navbar navbar-expand-lg navbar-dark bg-info">
        <div class="container">
          <%= link_to "農効", root_path, class:"navbar-brand" %>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
          <% if farmer_signed_in? %>
             <p class="pr-5 text-light"><%= current_farmer.farm_name %>  様</p>
            <li>
              <%= link_to "栽培区画一覧", farmer_farmlands_path, class:'nav-link text-light' %>
            </li>
            <li>
              <%= link_to "農薬一覧", farmer_pesticides_path, class:'nav-link text-light' %>
            </li>
            <li>
              <%= link_to "使用記録一覧", farmer_records_path, class:'nav-link text-light' %>
            </li>
            <li>
              <%= link_to "マイページ", farmer_current_farmer_path(current_farmer), class:'nav-link text-light' %>
            </li>
            <li>
              <%= link_to "ログアウト", destroy_farmer_session_path, method: :delete, class:'nav-link text-light' %>
            </li>
                     
          <% else %>
            <li>
              <%= link_to "新規登録", new_farmer_registration_path, class:'nav-link text-light' %>
            </li>
            <li>
              <%= link_to "ログイン", new_farmer_session_path, class:'nav-link text-light' %>
            </li>
          <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>
    <main>
      <div>
        <%= flash[:notice] %>
      </div>
      <%= yield %>
    </main>
    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p>Noukou Cumpany  noukou@maill.com</p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
