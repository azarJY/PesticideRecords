<!DOCTYPE html>
<html>
  <head>
    <title>Noukou</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/flatpickr/dist/flatpickr.min.css">
    <script src="https://cdn.jsdelivr.net/npm/flatpickr/dist/l10n/ja.js"></script>
    <script src="https://kit.fontawesome.com/1f515556ad.js" crossorigin="anonymous"></script>
    <style>
    @import url('https://fonts.googleapis.com/css2?family=Yuji+Boku&display=swap');

    .custom-font {
      font-family: "Yuji Boku", serif;
      font-weight: 400;
      font-style: normal;
    }

    .fsize48 {
      font-size: 48px;
    }
  </style>
  </head>

  <body class="custom-font">
    <header style="background-image: url('<%= asset_path('strowberry.jpg') %>'); background-size: cover; background-position: center; height: 150px; position: relative;">
      <nav class="navbar navbar-expand-lg navbar-dark bg-info" style="background-color: transparent !important;">
        <div class="container">
          <h1><%= link_to "農効", root_path, class: "navbar-brand fsize48" %></h1>
          <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarNavDropdown">
            <ul class="navbar-nav ml-auto">
              <% if farmer_signed_in? %>
                <li class="nav-item">
                  <div class="text-light mx-5 fsize48"><%= current_farmer.farm_name %></div>
                </li>
                <li class="nav-item">
                  <%= link_to "栽培区画一覧", farmer_farmlands_path, class:'nav-link text-info btn btn-light mt-5 ml-2' %>
                </li>
                <li class="nav-item">
                  <%= link_to "農薬一覧", farmer_pesticides_path, class:'nav-link text-warning btn btn-light mt-5 ml-2' %>
                </li>
                <li class="nav-item">
                  <%= link_to "使用記録一覧", farmer_records_path, class:'nav-link text-success btn btn-light mt-5 ml-2' %>
                </li>
                <li class="nav-item">
                  <%= link_to "マイページ", farmer_current_farmer_path(current_farmer), class:'nav-link text-dark btn btn-light mt-5 ml-2' %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログアウト", destroy_farmer_session_path, method: :delete, "data-confirm" => "ログアウトしますか？", class:'nav-link text-danger btn btn-light mt-5 ml-2' %>
                </li>
              <% else %>
                <li class="nav-item">
                  <%= link_to "新規登録", new_farmer_registration_path, class:'nav-link text-dark btn btn-light mt-5 ml-2' %>
                </li>
                <li class="nav-item">
                  <%= link_to "ログイン", new_farmer_session_path, class:'nav-link text-dark btn btn-light mt-5 ml-2' %>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
    </header>

    <main>
      <div class="container pt-5">
        <%= flash[:notice] %>
        <%= yield %>
      </div>
    </main>

    <footer>
      <div class="container mt-5">
        <div class="row">
          <div class="mx-auto">
            <p class="custom-font"><i class="fa-solid fa-wheat-awn"></i><i class="fa-solid fa-wheat-awn"></i>日本農効株式会社 <i class="fa-solid fa-tractor"></i></p>
          </div>
        </div>
      </div>
    </footer>
  </body>
</html>
